# ====================================================
# MONOLIBRARY ENVIRONMENT CONFIGURATION
# ====================================================
# Copy file ini ke .env dan sesuaikan dengan konfigurasi server Anda
# Command: cp .env.example .env
# ====================================================

# ============ DATABASE CONFIGURATION ============
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password_here
DB_DATABASE=monolibrary
DB_PORT=3306

# ============ SERVER CONFIGURATION ============
PORT=3000
NODE_ENV=production

# ============ JWT CONFIGURATION ============
# Generate secret key yang kuat dengan command:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRY=1d

# ============ CORS CONFIGURATION (Optional) ============
# Uncomment jika frontend di domain berbeda
# CORS_ORIGIN=http://yourdomain.com
